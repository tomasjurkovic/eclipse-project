package files;

import io.restassured.path.json.JsonPath;

public class ReusableMethods {

	public static JsonPath reqToJson(String response) {
		JsonPath jp = new JsonPath(response);
		return jp;
	}
}
